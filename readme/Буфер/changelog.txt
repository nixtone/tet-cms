Создать таблицу FIELDS
TITLE, DESCRIPTION, KEYWORDS
Шаблоны <!-- $TITLE$ -->

// $FOLDER = array_map('strtolower', $FOLDER);

function akfirst($array) {
	return $array[array_key_first($array)];
}

php_value date.timezone "Europe/Moscow"

umask(0);

# В процессе:
- Доразработка класса пользователя

БД  
- Обработка входных данных  
- Адекватный ответ методов CRUD

# Система:
- разделов (втч категорий)
- адресов
- шаблоны страниц
- временные файлы
- Админка
- Установщик
- единица информации (блок-элемент, так и класс-товар напрамер)
- "Список" (переменная $LIST)
- Текущий (CURRENT)
- DI

# Отдельные модули:
- интернет магазин
- отправка почты, смс

# Требуемые PHP расширения
- ARGON2

# В установщике
- наладить загрузку дампа
- борьба с ошибками

# Помимо того:
- Ведение документации и сопроводительной записки в "readme.md"
- Ведение ".gitignore" (внести туда config.php)

